# Given vector
given_vector <- c(10, 5, 3, 8, 20, 15, 6)

# Find the maximum value
max_value <- max(given_vector)

# Find the minimum value
min_value <- min(given_vector)

# Print the results
print(paste("Maximum value:", max_value))
print(paste("Minimum value:", min_value))
